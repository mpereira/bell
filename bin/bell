#!/usr/bin/env ruby

$:.unshift File.join(File.dirname(__FILE__), '..', 'lib')
require 'bell'

if ARGV.empty?
  puts Bell::USAGE
  exit
else
  case ARGV.first
  when 'user' then Bell::UserHandler.new(ARGV).parse_args
  end
end
