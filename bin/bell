#!/usr/bin/env ruby

$:.unshift File.join(File.dirname(__FILE__), '..', 'lib')
require 'bell'

if $0 == __FILE__
  begin
    Bell::CliHandler.new(STDOUT).handle!(ARGV)
  rescue Bell::Errors::CliHandlerArgumentError
    STDOUT.puts Bell::OutputFormatter.usage
  end
end
