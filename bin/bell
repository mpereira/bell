#!/usr/bin/env ruby

$:.unshift File.join(File.dirname(__FILE__), '..', 'lib')
require 'bell'

if ARGV.empty?
  puts Bell::USAGE
  exit
else
  case ARGV.first
  when 'user' then Bell::User::Handler.new(ARGV[1..-1]).parse_args
  end
end
